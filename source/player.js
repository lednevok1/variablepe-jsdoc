/**
 * @class Player
 * @hideconstructor
 * @summary Static functions for accessing other players
 */
const Player = function() {} 

/**
* @function 
* @summary Checks if player is local player (you)
* @argument {number} id
* @returns {boolean} Is local player or not
*/
Player.isLocalPlayer = function(id) {
    return;
}

/**
* @function
* @summary Returns player's nametag
* @argument {number} id
* @returns {string} nametag
*/
Player.getNameTag = function(id) {
    return;
}

/**
* @function
* @summary Sets player's nametag
* @argument {number} id
* @argument {string} nametag 
*/
Player.setNameTag = function(id, nametag) {
    return;
}

/**
* @function
* @summary Returns player's head yaw
* @argument {number} id
* @returns {number} Player's yaw
*/
Player.getYaw = function(id) {
    return;
}

/**
* @function
* @summary Returns player's head pitch
* @argument {number} id
* @returns {number} Player's pitch
*/
Player.getPitch = function(id) {
    return;
}

/**
* @function
* @summary Returns player's position X
* @argument {number} id
* @returns {number} X position
*/
Player.getPositionX = function(id) {
    return;
}

/**
* @function
* @summary Returns player's position Y
* @argument {number} id
* @returns {number} Y position
*/
Player.getPositionY = function(id) {
    return;
}

/**
* @function
* @summary Returns player's position Z
* @argument {number} id
* @returns {number} Z position
*/
Player.getPositionZ = function(id) {
    return;
}

/**
* @function
* @summary Returns player's X velocity
* @argument {number} id
* @returns {number} X velocity
*/
Player.getVelocityX = function(id) {
    return;
}

/**
* @function
* @summary Returns player's Y velocity
* @argument {number} id
* @returns {number} Y velocity
*/
Player.getVelocityY = function(id) {
    return;
}

/**
* @function
* @summary Returns player's Z velocity
* @argument {number} id
* @returns {number} Z velocity
*/
Player.getVelocityZ = function(id) {
    return;
}

/**
* @function
* @summary Returns player's hitbox size
* @argument {number} id
* @returns {number[]} Hitbox sizes
*/
Player.getCollisionSize = function(id) {
    return;
}

/**
* @function
* @summary Sets player's collision size
* @argument {number} id
* @argument {number} horizontal
* @argument {number} vertical
*/
Player.setCollisionSize = function(id, horizontal, vertical) {
    return;
}

/**
* @function
* @summary Sets player's shadow radius
* @argument {number} id
* @argument {number} radius
*/
Player.setShadowRadius = function(id, radius) {
    return;
}

/**
* @function
* @summary Gets player's flag status
* @argument {number} id
* @argument {number} flag
* @returns {boolean} Flag's status 
*/
Player.getStatusFlag = function(id, flag) {
    return;
}

/**
* @function
* @summary Returns player's fall distance
* @argument {number} id
* @returns {number} Fall distance
*/
Player.getFallDistance = function(id) {
    return;
}

/**
* @function
* @summary Checks if player is in creative mode
* @argument {number} id
* @returns {boolean} In creative mode or not
*/
Player.isInCreativeMode = function(id) {
    return;
}

/**
* @function
* @summary Checks if player is in lava
* @argument {number} id
* @returns {boolean} In lava or not
*/
Player.isInLava = function(id) {
    return;
}

/**
* @function
* @summary Checks if player has invisibility effect
* @argument {number} id
* @returns {boolean} Invisible or not
*/
Player.isInvisible = function(id) {
    return;
}

/**
* @function
* @summary Checks if player is in wall
* @argument {number} id
* @returns {boolean} In wall or not
*/
Player.isInWall = function(id) {
    return;
}

/**
* @function
* @summary Checks if player is in water
* @argument {number} id
* @returns {boolean} In water or not
*/
Player.isInWater = function(id) {
    return;
}

/**
* @function
* @summary Checks if player is in world
* @argument {number} id
* @returns {boolean} In world or not
*/
Player.isInWorld = function(id) {
    return;
}

/**
* @function
* @summary Checks if player is on fire
* @argument {number} id
* @returns {boolean} On fire or not
*/
Player.isOnFire = function(id) {
    return;
}

/**
* @function
* @summary Checks if player is on ground
* @argument {number} id
* @returns {boolean} On ground or not
*/
Player.isOnGround = function(id) {
    return;
}

/**
* @function
* @summary Checks if player is falling
* @argument {number} id
* @returns {boolean} Falling or not
*/
Player.isFalling = function(id) {
    return;
}

/**
* @function
* @summary Checks if player is immobile
* @argument {number} id
* @returns {boolean} Immobile or not
*/
Player.isImmobile = function(id) {
    return;
}

/**
* @function
* @summary Checks if player is sitting
* @argument {number} id
* @returns {boolean} Sitting or not
*/
Player.isSitting = function(id) {
    return;
}

/**
* @function
* @summary Checks if player is sneaking
* @argument {number} id
* @returns {boolean} Sneaking or not
*/
Player.isSneaking = function(id) {
    return;
}

/**
* @function
* @summary Checks if player is alive
* @argument {number} id
* @returns {boolean} Alive or not
*/
Player.isAlive = function(id) {
    return;
}

/**
* @function
* @summary Checks if player is on ladder
* @argument {number} id
* @returns {boolean} Is on ladder or not
*/
Player.isOnLadder = function(id) {
    return;
}

/**
* @function
* @summary Checks if player is able to fly
* @argument {number} id
* @returns {boolean} Can fly or not
*/
Player.canFly = function(id) {
    return;
}

/**
* @function
* @summary Checks if player's nametag can be shown
* @argument {number} id
* @returns {boolean} Can show nametag or not
*/
Player.canShowNameTag = function(id) {
    return;
}